import { PrepareUriPipe } from './prepare-uri.pipe';

describe('PrepareUriPipe', () => {
  it('create an instance', () => {
    const pipe = new PrepareUriPipe();
    expect(pipe).toBeTruthy();
  });
});
